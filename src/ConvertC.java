/*
    Takes a Celsius temperature parameter and returns the Fahrenheit conversion
 */

public class ConvertC {

    public static double convertC(double celsius){
        return (9 * (celsius / 5) + 32);
    }
}
